   main = function(args) {
       var n = require('readline-sync').question('podaj liczbe:');
       for (var i = 0; i < n; i++) {
           {
               console.info(Math.PI);
           };
       }
   };

   main(null);