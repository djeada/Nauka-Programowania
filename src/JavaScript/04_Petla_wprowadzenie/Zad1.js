   main = function(args) {
       var a = 0;
       while ((a !== 7)) {
           {
               var a = require('readline-sync').question('podaj liczbe:');
           }
       };
   };

   main(null);